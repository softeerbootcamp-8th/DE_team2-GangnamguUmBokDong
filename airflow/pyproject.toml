[project]
name = "airflow-dags"
version = "0.1.0"
requires-python = "==3.11.*"
dependencies = [
    "apache-airflow==2.10.4",
    "core",
    "psycopg2-binary>=2.9.12",
]

[tool.uv]
package = false

[tool.uv.sources]
core = { path = "../libs/core", editable = true }
