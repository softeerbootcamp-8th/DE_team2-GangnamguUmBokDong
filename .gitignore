# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env
.env
.env.local

# Node (apps/web)
node_modules/
dist/
.vite/

# OS
.DS_Store

# ML
models/
data/

# Data 
*.csv
*.parquet
*.db
data/

# log
*.log

# Airflow (AIRFLOW_HOME=airflow/ 로 컨테이너에서 생성되는 런타임 산출물)
airflow/logs/
airflow/resource-profiles/
airflow/airflow.cfg
airflow/airflow-webserver.pid
airflow/webserver_config.py
airflow/standalone_admin_password.txt


# EMR 패키징 임시 디렉터리 (make emr-package)
.emr-stage/

# Terraform
# .terraform.lock.hcl은 커밋한다 — provider 버전을 팀 전체가 고정해야 한다.
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfplan
terraform/terraform_override.tf
# 환경/팀마다 다르고 IP·버킷 이름이 들어간다. *.example만 커밋한다.
terraform/backend.hcl
terraform/terraform.tfvars
# make allow-my-ip가 현재 공인 IP로 다시 쓴다.
terraform/admin_cidrs.auto.tfvars

# Claude
docs/superpowers/

# development
dev/
.claude/
.superpowers/

# Data
seoul-pop-nowcasting/data/

# ops scripts 런타임 산출물
ops/scripts/logs/

raw-data/
