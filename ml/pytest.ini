[pytest]
# 코드 정확성 검증용(pytest) 파일은 dev_*.py로 통일한다 — ML의 train/valid/**test**
# split(TEST_START, multi_horizon_test.parquet 등)과 "test_"가 겹쳐서 헷갈리는 걸
# 피하기 위함. pytest 기본 패턴(test_*.py)은 안 쓰므로 여기서 명시적으로 override.
python_files = dev_*.py
