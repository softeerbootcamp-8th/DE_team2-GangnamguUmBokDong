[project]
name = "collector"
version = "0.1.0"
requires-python = "==3.11.*"
dependencies = [
    "core",
    "requests>=2.34.2",
]

[tool.uv]
package = false

[tool.uv.sources]
core = { path = "../libs/core", editable = true }
